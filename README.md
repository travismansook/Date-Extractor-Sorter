# Date-Extractor-Sorter

•	Built a Bash tool that extracts dates in multiple formats (MM/DD/YYYY, MM-DD-YYYY, MM.DD.YYYY) from a text file using regex
•	Normalized mixed date formats into sortable keys to output all extracted dates in chronological order
